<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>OTP Verification</title>
    <style>
      body {
        margin: 0;
        padding: 0;
        background-color: #f4f6f8;
        font-family: "Segoe UI", Arial, sans-serif;
      }
      .email-wrapper {
        width: 100%;
        padding: 40px 0;
        background-color: #f4f6f8;
        text-align: center;
      }
      .email-container {
        background-color: #ffffff;
        max-width: 520px;
        margin: 0 auto;
        padding: 40px 30px;
        border-radius: 12px;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
      }
      .brand {
        font-size: 26px;
        font-weight: 700;
        color: #2b7cff;
        letter-spacing: 1px;
        margin-bottom: 10px;
      }
      h2 {
        color: #111827;
        font-size: 22px;
        margin-top: 20px;
        margin-bottom: 10px;
      }
      p {
        color: #4b5563;
        font-size: 15px;
        line-height: 1.6;
        margin: 10px 0;
      }
      .otp-box {
        display: inline-block;
        margin: 20px 0;
        padding: 12px 24px;
        background-color: #f0f7ff;
        border-radius: 8px;
        font-size: 32px;
        font-weight: bold;
        color: #2b7cff;
        letter-spacing: 6px;
      }
      .footer {
        font-size: 12px;
        color: #9ca3af;
        margin-top: 35px;
        border-top: 1px solid #e5e7eb;
        padding-top: 15px;
      }
      @media only screen and (max-width: 600px) {
        .email-container {
          width: 90%;
          padding: 30px 20px;
        }
        .otp-box {
          font-size: 26px;
          letter-spacing: 4px;
        }
      }
    </style>
  </head>
  <body>
    <div class="email-wrapper">
      <div class="email-container">
        <div class="brand">NEOPAY</div>

        <h2>Hello <%= name %>,</h2>

        <p>
          Thank you for using <strong>NEOPAY</strong>. Your One-Time Password
          (OTP) is:
        </p>

        <div class="otp-box"><%= otp %></div>

        <p>
          This OTP is valid for <strong>2 minutes</strong>. For your security,
          do not share this code with anyone.
        </p>

        <div class="footer">
          © <%= new Date().getFullYear() %> NEOPAY. All rights reserved.
        </div>
      </div>
    </div>
  </body>
</html>
